# These values override the safe production defaults when the deployment
# topology changes. The current native deployment uses the values below.
VITE_BASE_PATH=/rightnow/

# Nginx forwards this public prefix to the private NestJS API.
VITE_API_BASE_URL=/rightnow-api
